type: datasources.components.DltLoadSourceCollection

attributes:
  loads:
    - source: .loader.source
      pipeline: .loader.pipeline
      partitions_def:
        type: daily
        start_date: "2026-06-01"
      backfill_policy:
        type: single_run
      translation:
        key: "adam/{{ resource.name }}"
        group_name: adam